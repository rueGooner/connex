import { render, screen, waitFor } from '@testing-library/react';
import { useMetricsData } from './useMetricsData';
import { act } from 'react-dom/test-utils';

jest.mock('node-fetch', () => jest.fn());

describe('useMetricsData hook', () => {
  it('')
})
